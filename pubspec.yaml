name: layer_shell
description: Create panels and other desktop components for Wayland using the
  Layer Shell protocol — powered by Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.3

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
